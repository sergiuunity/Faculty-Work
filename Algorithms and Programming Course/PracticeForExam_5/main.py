#2
#a = 2
#a = 1

#3
#my_list = [234, 121, 123, 512]
#my_list.sort(key=lambda number: number % 10)
#print(my_list)

#4
# In black box testing the tester does not have access to the code while in white box testing he does.

#5
#1,3,5

#6
#Exception
#[0.5, 2.0, 3]
#because we will get the ZeroDivisionError when i = 2

#7
#return [n%10] + f(n//10)

#8
#b,c,d

#9
#returns a list containing all the elements in reverse order
#assert f([1,2,3]) == [3,2,1]
#assert f([]) == []
#assert (["b", "c", "a"]) == ["a","c","b"]

#10
# lst = ["abc", "The ds", "Thed"]
# print(list(filter(lambda string: type(string) == str and string.startswith("The"), lst)))